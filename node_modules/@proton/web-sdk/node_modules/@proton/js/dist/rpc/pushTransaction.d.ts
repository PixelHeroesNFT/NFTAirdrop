import { JsonRpc } from './index';
import { PushTransactionArgs } from './types';
/** Push a serialized transaction (replaced by send_transaction, but returned format has changed) */
export declare function push_transaction(this: JsonRpc, { signatures, compression, serializedTransaction, serializedContextFreeData }: PushTransactionArgs): Promise<any>;
/** Send a serialized transaction */
export declare function send_transaction(this: JsonRpc, { signatures, compression, serializedTransaction, serializedContextFreeData }: PushTransactionArgs): Promise<any>;
